services:
  - type: web
    name: helpbot-api
    runtime: python
    buildCommand: pip install -r requirements.txt && python rag/indexer.py
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false